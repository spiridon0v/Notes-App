import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  ScrollView: {
    width: '100%',
    height: '100%',
    paddingTop: 20,
  },
  ContentContainer: {
    alignItems: 'center',
  },
});
